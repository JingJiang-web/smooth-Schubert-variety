In E-poincare_polynomial.py, we input w in line 39, and determine the smoothness of X(w_0w) using the Poincaré polynomial. The algorithm runs efficiently only in type E6.

In Smooth-E.py, we input w and determine the smoothness of X(w_0w) in type E.

chv1r6180.py is a package.

longest_element.py can return the longest element of a Weyl group. (We use Geck's notation form).

In smoothBCD.py, we input the one-line notation of w, and determine the smoothness of X(w).
